# This is an example file. Copy this to .env and fill in your actual credentials.
# The .env file should NOT be committed to version control.
 
# --- Server Configuration ---
# The port the server will run on. Defaults to 3001 if not set.
# PORT=3001
 
# Set to 'production' when deploying to a live server.
# This enables features like secure cookies.
# NODE_ENV=production
 
# --- MongoDB Connection ---
MONGO_URI=mongodb+srv://<user>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority
 
# --- Cloudinary Credentials ---
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
 
# --- JWT Authentication ---
JWT_SECRET=your-super-secret-and-long-random-string # Example: wJalrXUtnFEMI/K7MD/ssN6gq5k7m3v5S3x+sE+4b/U=
 
# --- Nodemailer (Email) Credentials ---
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password